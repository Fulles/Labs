#include <stdio.h>
#include <math.h>
int main(void){

    double x,y;
    int n;
    scanf("%lf", &x);
    for (;;) {
        x = x+0,1;
        y = 1*x;
        printf("%lf", &y);
        if ((x < x+(n*0,1))) continue;
        else break;

    }



}
